{
  "name": "mflorenzocypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "prod": "npx cypress run --env ENV=prod --headless --browser chrome",
    "stage": "npx cypress run --env ENV=stage --headless --browser chrome"
  },
  "author": "maria florencia lorenzo",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.1.0"
  }
}
