{
  "name": "mflorenzocypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "prod": "npx cypress run --env configFile=prod --headless --browser chrome",
    "qa": "CYPRESS_BASE_URL=https://staging.app.com npx cypress run --env configFile=qa --headless --browser chrome"
  },
  "author": "maria florencia lorenzo",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.1.0"
  }
}
